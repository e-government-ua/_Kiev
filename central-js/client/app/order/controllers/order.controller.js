angular.module('order', []).controller('OrderController', function($scope, $state, $location, $window,MessagesService, BankIDService) {
    //if ($state.is('index.order')) {
    //    return $state.go('index.order.search');
    //}
});
