'use strict';

var should = require('should');
var app = require('../../app');
var request = require('supertest');

describe('POST /api/process-form/submit', function() {
  it('should respond with 200', function(done) {
    done();
  });
});
